```{r}
install.packages( "qualR")
```
---
title: "Groundwater Quality Assessment Report"
author: "Your Name / Your Organization"
date: "`r Sys.Date()`"
output:
  html_document:
    toc: true
    toc_float: true
    theme: united
---

```{r setup, include=FALSE}
# This chunk sets up our R environment for the report.
# 'include=FALSE' means this code will run, but won't appear in the final report.

knitr::opts_chunk$set(
  echo = FALSE,      # Don't show the R code in the final report
  message = FALSE,   # Don't show any package messages
  warning = FALSE,   # Don't show any warnings
  fig.align = 'center' # Center our plots
)

# Load the libraries we need
library(dplyr)
library(qualR)
library(knitr)
